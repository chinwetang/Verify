<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="om.cn.verifylibrary">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".UnLockGestureActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".SetGestureActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
    </application>

</manifest>
